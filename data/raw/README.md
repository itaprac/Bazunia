# Raw Data Sources

This directory stores raw JSON input files used by conversion scripts.

- `pytania_SI_sorted.json` -> input for `scripts/convert-si.js`
- `pytania_POI.json` -> raw POI dataset (for future conversion/import)

Files in `data/` (outside `raw/`) are app-ready datasets.
