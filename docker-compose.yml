services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    environment:
      BAZUNIA_CONVEX_URL: ${BAZUNIA_CONVEX_URL:-}
      BAZA_CONVEX_URL: ${BAZA_CONVEX_URL:-}
      BAZUNIA_PUBLIC_DECK_PROVIDER: ${BAZUNIA_PUBLIC_DECK_PROVIDER:-static}
    command: >
      /bin/sh -c
      "envsubst < /usr/share/nginx/html/js/runtime-config.template.js > /tmp/runtime-config.js
      && nginx -g 'daemon off;'"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./docs.html:/usr/share/nginx/html/docs.html:ro
      - ./css:/usr/share/nginx/html/css:ro
      - ./js:/usr/share/nginx/html/js:ro
      - ./data:/usr/share/nginx/html/data:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
